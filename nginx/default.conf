server {
    listen 80;

    location /site1/ {
        rewrite ^/site1/(.*)$ /$1 break;
        proxy_pass http://site1;
    }

    location /site2/ {
        rewrite ^/site2/(.*)$ /$1 break;
        proxy_pass http://site2;
    }
}